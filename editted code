


	




hit = input("To draw your first 2 cards type 2: ")
if hit == ("2"):
	url = "https://deckofcardsapi.com/api/deck/new/draw/?count=2"
	payload={}
	headers = {}
	firstcards = requests.request("GET", url, headers=headers, data=payload)
	print(firstcards.text)



url = "https://deckofcardsapi.com/api/deck/" + str(deck) "/draw/?count=2"
payload={}
headers = {
  'Accept': 'application/json'
}

FC = requests.request("GET", url, headers=headers, data=payload)
cards1 = FC.json()
FCvalue = card1v["value"]
FCsuit = cards1s["suit"]

print("Your cards are " + str(FCvalue) + " and contains " + str(FCsuit) + " decks.")



